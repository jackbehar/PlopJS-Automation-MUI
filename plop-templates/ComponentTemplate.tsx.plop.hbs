import { ComponentPropsWithRef } from "react";
import { {{pascalCase name}} as MUIComponent, {{pascalCase name}}Props } from "@mui/material";

interface MergeProps extends Omit<{{pascalCase name}}Props, keyof ComponentPropsWithRef<'button'>> {
  //Add additional prop definitions here
}
/**
 * @uxpindocurl https://mui.com/{{muiDocumentation}}/
 */
const {{pascalCase name}} = (props: MergeProps) => {
  return (
      <MUIComponent {...props}/>
  );
};

export default {{pascalCase name}};